import { palette } from '../../constants/palette';

export const layoutStyles = {
    stage:  { flex: 1, backgroundColor: palette.paper },
    scroll: { paddingHorizontal: 22, paddingTop: 26, paddingBottom: 52 },
};