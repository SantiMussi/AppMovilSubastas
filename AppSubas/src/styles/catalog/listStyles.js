import { palette } from '../../constants/palette';

export const listStyles = {
    listHeader:     { color: palette.ink, fontSize: 10, fontWeight: '900', letterSpacing: 1.4, marginBottom: 14 },
    separator:      { height: 1, backgroundColor: '#E8E4DC', marginVertical: 12 },
    emptySection:   { paddingVertical: 32, alignItems: 'center' },
    emptyText:      { color: palette.muted, fontSize: 12 },
};