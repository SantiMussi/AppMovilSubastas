import { palette } from '../../constants/palette';

export const buttonStyles = {
  buttonRow: {
    flexDirection: 'row',
    gap: 14,
    alignItems: 'center',
    marginTop: 20,
    justifyContent: 'flex-end',
  },
  footerNote: {
    fontSize: 9,
    letterSpacing: 1.5,
    color: palette.muted,
    textAlign: 'center',
    marginBottom: 10,
    marginTop: 40,
  },
};